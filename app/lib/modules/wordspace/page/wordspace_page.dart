import 'package:flutter/material.dart';

class WordspacePage extends StatelessWidget {
  const WordspacePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
