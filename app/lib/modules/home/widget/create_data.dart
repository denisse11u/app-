import 'package:flutter/material.dart';

class CreateData extends StatelessWidget {
  const CreateData({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("nuevo texto")));
  }
}
